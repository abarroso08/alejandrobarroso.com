<header class="navbar">
  <nav aria-label="Main navigation">
    <a class="home" href="/">AB<span>.</span></a>
    <a href="/projects">Projects</a>
    <a href="/books">Books</a>
    <a href="/about">About</a>
  </nav>
</header>

<style>
  .navbar { padding: var(--space-2) var(--space-4); }
  nav { display: flex; align-items: center; gap: var(--space-4); max-width: var(--wide-width); margin: 0 auto; }
  nav a { color: var(--color-text-muted); }
  nav a:hover { color: var(--color-accent); }
  .home { color: var(--color-text); font-weight: 650; margin-right: auto; }
  .home span { color: var(--color-accent); }
  @media (max-width: 700px) { nav { gap: var(--space-3); } nav a:not(.home) { font-size: .9rem; } }
</style>
